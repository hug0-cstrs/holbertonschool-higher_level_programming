#!/usr/bin/python3
def multiple_returns(sentence):
    if length == 0:
        tuple_return = (len(sentence), None)
        return tuple_return
    tuple_return = (len(sentence), sentence[0])
    return tuple_return
